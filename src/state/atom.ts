import { atom } from "recoil"

export const assetsList = atom<any[]>({
  key: 'assetsList',
  default: []
})

export const unitsList = atom<any[]>({
  key: 'unitsList',
  default: []
})

export const companiesList = atom<any[]>({
  key: 'companiesList',
  default: []
})

export const usersList = atom<any[]>({
  key: 'companiesList',
  default: []
})